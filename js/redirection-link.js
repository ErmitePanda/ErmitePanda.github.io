document.addEventListener('DOMContentLoaded', function () {
    const allLink = document.querySelectorAll('.link-nav > div > a');

    const about = allLink[0];
    const work = allLink[1];
    const skill = allLink[2];
    const contact = allLink[3];
});